<?php

declare(strict_types=1);

/**
 * Static-analysis stub for the call functions of the CallTest units: the
 * real declarations live in unit files generated under the system temp
 * directory when the tests run, which PHPStan cannot see. This file is listed
 * in scanFiles only — nothing loads it, and phpunit ignores it (it does not
 * match the *Test.php suffix).
 *
 * @internal
 */

use Pure\Component\Call;

function FluentCard(mixed ...$children): Call
{
    throw new LogicException('scan stub: never loaded');
}

function FluentPlain(mixed ...$children): Call
{
    throw new LogicException('scan stub: never loaded');
}

function FluentArtifact(mixed ...$children): Call
{
    throw new LogicException('scan stub: never loaded');
}

function FluentDeclared(mixed ...$children): Call
{
    throw new LogicException('scan stub: never loaded');
}

function FluentSection(mixed ...$children): Call
{
    throw new LogicException('scan stub: never loaded');
}
